//imports contents of one file into this source file
#include <stdio.h>

//needs to return an int for the argc and an array for the argv
int main(int argc, char *argv[]){
	int distance=100;
	
	//prints a string with an int value assigned from 
	printf("You are %d miles away.\n", distance);
	
	//gives an exit value? look into this
	return 0;
}